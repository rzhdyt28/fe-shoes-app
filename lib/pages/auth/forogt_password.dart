import 'package:flutter/material.dart';

class ExampleWidget extends StatelessWidget {
  const ExampleWidget({super.key});

  // Bedanya nulis widget disini
  @override
  Widget build(BuildContext context) {
    // sama disini apa am?
    return const Placeholder();
  }
}
